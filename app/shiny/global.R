options_tab = list(
    autoWidth = TRUE,
    columnDefs = list(list(
        className = 'dt-center', targets = 5
    )),
    pageLength = 5,
    dom = 'Bfrtipl',
    orientation = 'landscape',
    buttons = list('copy', 'csv', 'excel'),
    pageLength = 5
)